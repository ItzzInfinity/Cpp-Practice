# ✅ C++ Basics — Practice Checklist

> Work through these one by one. Check off each question as you complete it.  
> Goal: Build **confidence**, **speed**, and a strong grip on **syntax**.

---

## 🟢 Section 1 — Hello World & Output

- [x] Write a program that prints `Hello, World!`
- [x] Print your name using `cout`
- [x] Print two lines of text using two `cout` statements
- [x] Print a number (e.g. `42`) using `cout`
- [x] Print a decimal number (e.g. `3.14`) using `cout`
- [x] Print a single character (e.g. `'A'`) using `cout`
- [x] Print text with a tab space between two words using `\t`
- [x] Print text on a new line using `\n`
- [x] Print text on a new line using `endl`
- [x] Print a blank line between two sentences
- [x] Print the result of `5 + 3` directly inside `cout`
- [x] Print multiple things in one `cout` using `<<` chaining
- [x] Print your full name and age on separate lines
- [x] Print a sentence that includes a double quote character (use `\"`)
- [x] Print a backslash using `\\`

---

## 🟢 Section 2 — Variables & Data Types

- [x] Declare an `int` variable and print it
- [x] Declare a `float` variable and print it
- [x] Declare a `double` variable and print it
- [x] Declare a `char` variable and print it
- [x] Declare a `bool` variable, set it to `true`, and print it
- [x] Declare a `string` variable and print it
- [x] Declare two `int` variables and print both
- [x] Declare a variable, assign a value later (not at declaration), then print it
- [x] Declare a variable with `const` and try to understand why you can't change it
- [x] Print the size of `int` using `sizeof(int)`
- [x] Print the size of `double` using `sizeof(double)`
- [x] Print the size of `char` using `sizeof(char)`
- [x] Print the size of `bool` using `sizeof(bool)`
- [x] Declare an `unsigned int` and print it
- [x] Declare a `long` variable and print it
- [x] Declare a `long long` variable with a very large number and print it
- [x] Declare a `short` variable and print it
- [x] Initialize a variable using `auto` and let the compiler deduce the type
- [x] Declare two variables of the same type in one line (e.g. `int a = 1, b = 2;`)
- [x] Swap the values of two variables using a third temp variable

---

## 🟢 Section 3 — User Input

- [x] Read an integer from the user and print it back
- [x] Read a float from the user and print it back
- [x] Read a character from the user and print it back
- [x] Read a string (single word) using `cin` and print it
- [x] Read a full line of text using `getline()` and print it
- [x] Read two integers from the user and print both
- [x] Read the user's name and greet them: `Hello, [name]!`
- [x] Read two numbers and print their sum
- [x] Read a number and print it doubled
- [x] Read age from user and print: `You are [age] years old.`

---

## 🟢 Section 4 — Arithmetic Operators

- [ ] Add two numbers and print the result
- [ ] Subtract two numbers and print the result
- [ ] Multiply two numbers and print the result
- [ ] Divide two integers and print the result (notice integer division)
- [ ] Divide two floats and print the result
- [ ] Find the remainder of 17 divided by 5 using `%`
- [ ] Calculate `2` to the power of `10` using a loop (no library)
- [ ] Read two numbers and print all four: sum, difference, product, quotient
- [ ] Increment a variable using `++` (prefix)
- [ ] Increment a variable using `++` (postfix)
- [ ] Decrement a variable using `--`
- [ ] Use `+=` to add 5 to a variable
- [ ] Use `-=` to subtract 3 from a variable
- [ ] Use `*=` to double a variable
- [ ] Use `/=` to halve a variable
- [ ] Use `%=` on a variable and print the result
- [ ] Convert temperature from Celsius to Fahrenheit: `F = C * 9/5 + 32`
- [ ] Convert temperature from Fahrenheit to Celsius
- [ ] Find the average of three numbers entered by the user
- [ ] Calculate the area of a rectangle (length × width)
- [ ] Calculate the perimeter of a rectangle
- [ ] Calculate the area of a circle using `3.14159 * r * r`
- [ ] Calculate the circumference of a circle

---

## 🟢 Section 5 — Comparison & Logical Operators

- [ ] Check if 10 is greater than 5 and print the boolean result
- [ ] Check if two numbers entered by the user are equal using `==`
- [ ] Check if two numbers are not equal using `!=`
- [ ] Check if a number is greater than or equal to 18
- [ ] Check if a number is less than 0
- [ ] Use `&&` (AND): check if a number is between 1 and 100
- [ ] Use `||` (OR): check if a number is either 0 or negative
- [ ] Use `!` (NOT): negate a boolean variable and print it
- [ ] Print `1` or `0` for a comparison result using `cout`
- [ ] Use `boolalpha` to print `true`/`false` instead of `1`/`0`

---

## 🟢 Section 6 — Conditionals (if / else)

- [ ] Print "Positive" if a number is greater than 0
- [ ] Print "Positive" or "Negative" based on user input
- [ ] Print "Positive", "Negative", or "Zero" using if-else-if
- [ ] Check if a number is even or odd
- [ ] Check if a number is divisible by 3
- [ ] Check if a number is divisible by both 2 and 5
- [ ] Print the larger of two numbers
- [ ] Print the smaller of two numbers
- [ ] Check if a character is a vowel or consonant
- [ ] Check if a person is eligible to vote (age >= 18)
- [ ] Check if a year is a leap year
- [ ] Print grade based on marks: A (90+), B (80+), C (70+), D (60+), F (below 60)
- [ ] Use the ternary operator `? :` to print "Pass" or "Fail"
- [ ] Use nested if-else to determine the sign and whether a number is even/odd
- [ ] Check if a number is a multiple of 7

---

## 🟢 Section 7 — Switch Statement

- [ ] Use `switch` to print the day name for a number 1–7
- [ ] Use `switch` to print the month name for a number 1–12
- [ ] Use `switch` to perform a basic calculator (add/subtract/multiply/divide) based on operator input
- [ ] Use `switch` on a character to handle vowels
- [ ] Use `switch` with a `default` case for invalid input
- [ ] Use `switch` with fall-through intentionally and observe the output
- [ ] Use `switch` to print seasons based on month number

---

## 🟢 Section 8 — Loops (for)

- [ ] Print numbers 1 to 10 using a `for` loop
- [ ] Print numbers 10 to 1 (countdown) using a `for` loop
- [ ] Print all even numbers from 1 to 50
- [ ] Print all odd numbers from 1 to 50
- [ ] Print the multiplication table of 5
- [ ] Read a number and print its multiplication table
- [ ] Calculate the sum of numbers from 1 to 100
- [ ] Print the squares of numbers 1 to 10
- [ ] Print the cubes of numbers 1 to 10
- [ ] Print all multiples of 3 from 3 to 99
- [ ] Print the first 10 terms of the Fibonacci sequence
- [ ] Calculate `n!` (factorial) for a user-input number
- [ ] Print a countdown from 10 to 1 then "Blast off!"
- [ ] Print numbers divisible by both 3 and 5 up to 100
- [ ] Use a `for` loop to print each character of a string one by one

---

## 🟢 Section 9 — Loops (while)

- [ ] Print 1 to 10 using a `while` loop
- [ ] Keep asking the user for a number until they enter 0
- [ ] Count how many digits are in a number using `while`
- [ ] Reverse the digits of a number using `while`
- [ ] Find the sum of digits of a number using `while`
- [ ] Print the digits of a number one per line using `while`
- [ ] Keep reading numbers and sum them until user enters -1
- [ ] Print powers of 2 up to 1024 using `while`
- [ ] Check if a number is a palindrome using `while`
- [ ] Convert a decimal number to binary using `while`

---

## 🟢 Section 10 — Loops (do-while)

- [ ] Print 1 to 5 using a `do-while` loop
- [ ] Ask the user for a positive number; keep asking if they enter a negative one
- [ ] Create a simple menu that keeps showing until user picks "Exit"
- [ ] Use `do-while` to validate that user input is between 1 and 10
- [ ] Count up from 1 and stop when the sum exceeds 50

---

## 🟢 Section 11 — Nested Loops & Patterns

- [ ] Print a 5×5 grid of `*`
- [ ] Print a right-angled triangle of `*` (5 rows)
- [ ] Print an inverted right-angled triangle of `*`
- [ ] Print a number triangle (1, 12, 123, ...)
- [ ] Print a multiplication table from 1×1 to 10×10
- [ ] Print a square border of `*` (hollow square)
- [ ] Print a pyramid of `*` (centered)
- [ ] Print a diamond shape of `*`
- [ ] Print the pattern: 1, 22, 333, 4444, 55555
- [ ] Print alternating `*` and `#` in a 5×5 grid (checkerboard)

---

## 🟢 Section 12 — Functions (Basics)

- [ ] Write a function `greet()` that prints "Hello!"
- [ ] Write a function `add(int a, int b)` that returns the sum
- [ ] Write a function `multiply(int a, int b)` that returns the product
- [ ] Write a function `isEven(int n)` that returns `true` or `false`
- [ ] Write a function `printSquare(int n)` that prints `n * n`
- [ ] Write a function `max(int a, int b)` that returns the larger number
- [ ] Write a function `min(int a, int b)` that returns the smaller number
- [ ] Write a function `absolute(int n)` that returns absolute value
- [ ] Write a function `factorial(int n)` that returns n!
- [ ] Write a function `isPrime(int n)` that returns `true` if prime
- [ ] Write a function `power(int base, int exp)` that returns base^exp
- [ ] Write a function with no parameters and no return type
- [ ] Write a function that takes a string and prints it with `***` around it
- [ ] Write a function `celsiusToFahrenheit(float c)` that returns converted value
- [ ] Call a function multiple times with different arguments and print each result
- [ ] Write two functions that call each other in a meaningful way
- [ ] Write a function with a default parameter value
- [ ] Write an overloaded function: `add(int, int)` and `add(double, double)`
- [ ] Write a recursive function for countdown from n to 1
- [ ] Write a recursive function for factorial

---

## 🟢 Section 13 — Arrays (Basics)

- [ ] Declare an array of 5 integers and print all elements
- [ ] Read 5 integers into an array and print them
- [ ] Find the sum of all elements in an array
- [ ] Find the average of all elements in an array
- [ ] Find the maximum element in an array
- [ ] Find the minimum element in an array
- [ ] Print an array in reverse order
- [ ] Count how many elements in an array are even
- [ ] Count how many elements in an array are greater than 50
- [ ] Search for a specific number in an array (linear search)
- [ ] Copy one array into another
- [ ] Declare a 2D array (3×3) and print it as a grid
- [ ] Fill a 3×3 2D array with user input and print it
- [ ] Find the sum of a row in a 2D array
- [ ] Find the sum of a column in a 2D array
- [ ] Initialize an array with values at declaration
- [ ] Print only elements at even indices of an array
- [ ] Print only elements at odd indices of an array
- [ ] Replace all negative numbers in an array with 0
- [ ] Swap the first and last elements of an array

---

## 🟢 Section 14 — Strings (std::string)

- [ ] Declare a `string` and print its length using `.length()`
- [ ] Read a string and print its length
- [ ] Access and print the first character of a string using `[0]`
- [ ] Access and print the last character of a string
- [ ] Print each character of a string on a new line using a loop
- [ ] Check if two strings are equal using `==`
- [ ] Concatenate two strings using `+`
- [ ] Append to a string using `+=`
- [ ] Convert a string to uppercase manually using a loop
- [ ] Count how many vowels are in a string
- [ ] Count the spaces in a string
- [ ] Reverse a string using a loop
- [ ] Check if a string is a palindrome
- [ ] Find a character in a string using `.find()`
- [ ] Replace a character in a string
- [ ] Extract a substring using `.substr()`
- [ ] Compare two strings using `.compare()`
- [ ] Check if a string starts with a specific letter
- [ ] Check if a string ends with a specific letter
- [ ] Count how many times a character appears in a string

---

## 🟢 Section 15 — Pointers (Gentle Introduction)

- [ ] Declare an `int` variable and a pointer to it, then print the pointer's value
- [ ] Print the address of a variable using `&`
- [ ] Dereference a pointer using `*` and print the variable's value through it
- [ ] Change a variable's value through a pointer
- [ ] Declare a `nullptr` pointer and check if it's null before using it
- [ ] Pass a variable to a function using a pointer and modify it inside the function
- [ ] Create a pointer to an array and print the first element
- [ ] Use pointer arithmetic to iterate through an array
- [ ] Swap two numbers using pointers inside a function
- [ ] Print the size of a pointer using `sizeof`

---

## 🟢 Section 16 — References

- [ ] Create a reference to an integer variable and print it
- [ ] Modify a variable through its reference
- [ ] Pass a variable by reference to a function and modify it
- [ ] Write a `swap` function using references
- [ ] Understand the difference: pass by value vs pass by reference (write both versions)
- [ ] Create a `const` reference and try to understand why it can't be changed
- [ ] Return a reference from a function (simple example)

---

## 🟢 Section 17 — Structures (struct)

- [ ] Define a `struct Student` with name and age, create one instance and print it
- [ ] Create an array of 3 `struct Student` objects and print all
- [ ] Add a marks field to `Student` and print all fields
- [ ] Write a function that takes a `struct` and prints its details
- [ ] Write a function that returns a `struct`
- [ ] Nest one struct inside another (e.g. `Address` inside `Person`)
- [ ] Create a `struct Rectangle` with width and height, write a function to calculate its area
- [ ] Initialize a struct using designated initializers
- [ ] Compare two structs manually (field by field)
- [ ] Use a pointer to a struct and access fields with `->`

---

## 🟢 Section 18 — File I/O (Basics)

- [ ] Write "Hello, File!" to a text file using `ofstream`
- [ ] Read the contents of a text file and print them using `ifstream`
- [ ] Append a line to an existing file using `ofstream` in append mode
- [ ] Write multiple lines to a file using a loop
- [ ] Read a file line by line using `getline()` and print each line
- [ ] Write user input to a file
- [ ] Read integers from a file and print their sum
- [ ] Check if a file was opened successfully before reading/writing
- [ ] Count the number of lines in a text file
- [ ] Copy contents from one file to another

---

## 🟢 Section 19 — Math & Standard Library

- [ ] Use `abs()` to get the absolute value of a negative number
- [ ] Use `sqrt()` to find the square root of 144
- [ ] Use `pow(2, 8)` and print the result
- [ ] Use `ceil()` on `4.2` and print the result
- [ ] Use `floor()` on `4.9` and print the result
- [ ] Use `round()` on `4.5` and print the result
- [ ] Use `max(a, b)` from `<algorithm>` and print the result
- [ ] Use `min(a, b)` from `<algorithm>` and print the result
- [ ] Generate a random number using `rand()`
- [ ] Generate a random number between 1 and 100 using `rand() % 100 + 1`
- [ ] Seed the random number generator with `srand(time(0))`
- [ ] Simulate rolling a 6-sided dice 10 times and print results
- [ ] Use `log()` and `log10()` and print their results
- [ ] Use `sin()`, `cos()`, `tan()` for a given angle in radians
- [ ] Define and use the constant `M_PI` for circle calculations

---

## 🟢 Section 20 — Miscellaneous Challenges

- [ ] Print all prime numbers from 1 to 100
- [ ] Print the Fibonacci sequence up to 1000
- [ ] Check if a number is an Armstrong number (e.g. 153 = 1³+5³+3³)
- [ ] Print all Armstrong numbers between 1 and 999
- [ ] Find GCD of two numbers using a loop
- [ ] Find LCM of two numbers
- [ ] Check if a number is a perfect number (sum of divisors = itself)
- [ ] Print all perfect numbers between 1 and 1000
- [ ] Count the number of words in a sentence entered by the user
- [ ] Print the frequency of each digit (0–9) in a number
- [ ] Build a number guessing game (random number, user guesses, hints given)
- [ ] Build a simple calculator that loops until user enters 'q' to quit
- [ ] Print Pascal's triangle up to 6 rows
- [ ] Convert binary to decimal
- [ ] Convert decimal to binary
- [ ] Convert decimal to hexadecimal
- [ ] Check if a string has balanced parentheses (basic version with counter)
- [ ] Find the second largest number in an array
- [ ] Remove duplicates from an array (print unique elements only)
- [ ] Sort an array using bubble sort

---

> 💡 **Tip:** Don't rush. Write every program by hand, compile it, and fix errors yourself.  
> Each checkbox you tick is a real win. Keep going! 🚀
